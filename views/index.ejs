<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Glassmorphism effect */
        .glass {
            background: rgba(255, 255, 255, 0.1);
            /* Semi-transparent background */
            border-radius: 15px;
            /* Rounded corners */
            backdrop-filter: blur(10px);
            /* Background blur */
            border: 1px solid rgba(255, 255, 255, 0.2);
            /* Light border */
        }

        /* Animation on Create Account block */
        @keyframes slideIn {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }

            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .animate-in {
            animation: slideIn 0.5s ease-out;
        }

        /* Background styling */
        body {
            margin: 0;
            height: 100vh;
            background-image: url('https://via.placeholder.com/1500');
            /* You can add your background image here */
            background-size: cover;
            background-position: center;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }
    </style>
</head>

<body class="bg-zinc-900 text-white">
    <div class="absolute inset-0 bg-black opacity-60"></div> <!-- Background overlay for darker effect -->

    <div class="w-full min-h-screen flex justify-center items-center p-10">
        <!-- Create Account Block with Glassmorphism and Animation -->
        <div class="relative z-10 bg-zinc-800 p-8 rounded-lg shadow-lg glass w-full max-w-lg space-y-6 animate-in">
            <div class="flex justify-between w-full mb-8">
                <h3 class="text-3xl font-semibold">Create Account</h3>
                <a href="/login" class="px-4 py-2 bg-blue-500 rounded-md hover:bg-blue-400 transition">Login</a>
            </div>

            <form action="/register" method="post" class="space-y-6">
                <div>
                    <label for="username" class="block text-sm font-medium">Username</label>
                    <input id="username"
                        class="w-full px-4 py-3 mt-2 bg-transparent border-2 border-zinc-700 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
                        type="text" placeholder="Username" name="username" required>
                </div>

                <div>
                    <label for="name" class="block text-sm font-medium">Name</label>
                    <input id="name"
                        class="w-full px-4 py-3 mt-2 bg-transparent border-2 border-zinc-700 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
                        type="text" placeholder="Name" name="name" required>
                </div>

                <div>
                    <label for="email" class="block text-sm font-medium">Email</label>
                    <input id="email"
                        class="w-full px-4 py-3 mt-2 bg-transparent border-2 border-zinc-700 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
                        type="email" placeholder="Email" name="email" required>
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium">Password</label>
                    <input id="password"
                        class="w-full px-4 py-3 mt-2 bg-transparent border-2 border-zinc-700 rounded-md text-white focus:outline-none focus:ring-2 focus:ring-blue-500"
                        type="password" placeholder="Password" name="password" required>
                </div>

                <div>
                    <input class="w-full px-4 py-3 bg-blue-500 rounded-md cursor-pointer hover:bg-blue-400 transition"
                        type="submit" value="Create Account">
                </div>
            </form>
        </div>
    </div>
</body>

</html>
